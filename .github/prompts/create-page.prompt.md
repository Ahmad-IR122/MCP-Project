# Create New Page

Create a new React page for this project.

Requirements:

- Put the page inside `src/pages`.
- Use TypeScript.
- Use the existing Navbar.
- Keep the design consistent with the existing pages.
- Make the page responsive.
- Do not install new packages.
- Do not change unrelated files.